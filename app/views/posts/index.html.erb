<%= form_with model: @post, local: true do |f|%>
<%= f.text_field :content %>
<%= f.submit%>
<%end%>

<% @posts.each do |post| %>
    <p><%= post.content%> <%= link_to "削除", "posts/#{post.id}", method: :delete %><p/>
<% end %>
